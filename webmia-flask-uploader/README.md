# WebMIA Flask Uploader

Upload files through a flask-based app, which performs routing and rendering functions.

### How to Run
- Install dependencies in a virtual environment
  - `pip install -r requirements.txt`
- Run
  - `python mri-seg.py`
